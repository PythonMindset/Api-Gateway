const { body, param, query, validationResult } = require('express-validator');

const validateListPublicProjects = [
    // No specific validations needed for listing public projects
];

module.exports = validateListPublicProjects;